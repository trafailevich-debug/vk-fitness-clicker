import * as React from 'react';
import type { CustomSnackbar, SnackbarApi } from '../types';
import type { SnackbarStore } from './createSnackbarStore';
export type UseSnackbarActionsWithStoreProps = {
    store: SnackbarStore;
    limit: number;
    queueStrategy: 'queue' | 'shift';
    isDesktop: boolean;
};
export declare const useSnackbarActionsWithStore: ({ store, limit, queueStrategy, isDesktop, }: UseSnackbarActionsWithStoreProps) => {
    open: (config: SnackbarApi.OpenProps) => SnackbarApi.OpenReturn;
    openCustom: <AdditionalProps extends object = object>(config: CustomSnackbar.Payload<AdditionalProps> | React.ComponentType<CustomSnackbar.Props<AdditionalProps>>) => SnackbarApi.OpenReturn;
    update: (id: string, config: SnackbarApi.UpdateProps) => void;
    close: (id: string) => void;
    closeAll: () => void;
};
//# sourceMappingURL=useSnackbarActionsWithStore.d.ts.map