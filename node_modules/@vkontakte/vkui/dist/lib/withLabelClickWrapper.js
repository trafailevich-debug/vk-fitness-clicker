export const withLabelClickWrapper = (onClick)=>{
    if (onClick) {
        return (event)=>{
            if (event.target.tagName === 'INPUT') {
                onClick(event);
            }
        };
    }
    return undefined;
};

//# sourceMappingURL=withLabelClickWrapper.js.map