import { type DeepReadonly } from '../../../types';
import { type SVGPathSupport } from './path';
/**
 * Масштабирует путь.
 *
 * ```ts
 * import * as transform from './transform';
 * import * as assert from 'node:assert/strict';
 *
 * const scalePath = transform.scale(
 *   [['M', [10, 20]], ['L', [30, 40]]],
 *   2,
 *   2,
 * );
 *
 * assert.deepEqual(
 *   scalePath,
 *   [['M', [20, 40]], ['L', [60, 80]]],
 * );
 * ```
 */
export declare function scale(path: DeepReadonly<SVGPathSupport>, scaleX: number, scaleY: number): SVGPathSupport;
/**
 * Перемещает путь по осям X и Y.
 *
 * ```ts
 * import * as transform from './transform';
 * import * as assert from 'node:assert/strict';
 *
 * const translatePath = transform.translate(
 *   [['M', [10, 20]], ['L', [30, 40]]],
 *   10,
 *   20,
 * );
 *
 * assert.deepEqual(
 *   translatePath,
 *    [['M', [20, 40]], ['L', [40, 60]]],
 * );
 * ```
 */
export declare function translate(path: DeepReadonly<SVGPathSupport>, translateX: number, translateY: number): SVGPathSupport;
/**
 * Вращает путь относительно заданной точки на заданный угол
 *
 * ```ts
 * import * as transform from './transform';
 * import * as assert from 'node:assert/strict';
 *
 * const rotatePath = transform.rotate(
 *   [['M', [10, 20]], ['L', [30, 40]]],
 *   30,
 *   30,
 *   180
 * );
 *
 * assert.deepEqual(
 *   rotatePath,
 *   [['M', [20, 40]], ['L', [60, 80]]],
 * );
 * ```
 */
export declare function rotate(path: DeepReadonly<SVGPathSupport>, originX: number, originY: number, angle: number): SVGPathSupport;
//# sourceMappingURL=transform.d.ts.map