import { type DeepReadonly } from '../../../types';
import type { SVGPathSupport } from './path';
import { type Points } from './point';
export interface MaxSegmentLengthOptions {
    maxSegmentLength?: number;
}
/**
 * Аппроксимирует svg путь в точки
 *
 * ```ts
 * import * as approximate from './approximate';
 * import * as assert from 'node:assert/strict';
 *
 * assert.equal(
 *   approximate.approximateRing([['M', [20, 40]], ['L', [40, 60]]]),
 *   [[20, 40], [40, 60]],
 * );
 * ```
 */
export declare function approximateRing(path: DeepReadonly<SVGPathSupport>, { maxSegmentLength }?: MaxSegmentLengthOptions): Points;
//# sourceMappingURL=approximate.d.ts.map