import * as React from 'react';
import { type NavIdProps } from '../../lib/getNavId';
import type { HTMLAttributesWithRootRef } from '../../types';
export interface RootProps extends HTMLAttributesWithRootRef<HTMLDivElement>, NavIdProps {
    /**
     * `id` активной `View`.
     */
    activeView: string;
    /**
     * Обработчик, который вызывается при завершении анимации смены активной `View`.
     */
    onTransition?: ((params: {
        isBack: boolean;
        from: string;
        to: string;
    }) => void) | undefined;
    /**
     * Коллекция `View`. У каждой `View` должен быть `id`.
     */
    children: React.ReactElement | Iterable<React.ReactElement>;
}
export interface RootState {
    activeView: string;
    transition: boolean;
    isBack?: boolean | undefined;
    prevView?: string | undefined;
}
/**
 * @see https://vkui.io/components/root
 */
export declare const Root: ({ children, activeView: _activeView, onTransition: onTransitionProp, nav, ...restProps }: RootProps) => React.ReactNode;
//# sourceMappingURL=Root.d.ts.map